document.getElementById("about-button").addEventListener("click", function(event) {
    event.preventDefault(); // Prevent the default link behavior
    console.log("About button clicked"); // Check if this is logged
    
    // Existing code...
});
